class Zoominfo:
    @staticmethod
    def get_jwt_token():
        print("Zoominfo: Getting JWT token")

    @staticmethod
    def fetch_data_from_bq():
        print("Zoominfo: Fetching data from BQ")

    @staticmethod
    def create_payload():
        print("Zoominfo: Creating payload")

    @staticmethod
    def fetch_data_from_api():
        print("Zoominfo: Fetching data from API")

    @staticmethod
    def insert_data_to_bq():
        print("Zoominfo: Inserting data to BQ")

class Buildwith:
    @staticmethod
    def get_jwt_token():
        print("Buildwith: Getting JWT token")

    @staticmethod
    def fetch_data_from_bq():
        print("Buildwith: Fetching data from BQ")

    @staticmethod
    def create_payload():
        print("Buildwith: Creating payload")

    @staticmethod
    def fetch_data_from_api():
        print("Buildwith: Fetching data from API")

    @staticmethod
    def insert_data_to_bq():
        print("Buildwith: Inserting data to BQ")


import json
from airflow import DAG
from airflow.operators.dummy_operator import DummyOperator
from airflow.operators.python_operator import PythonOperator
from airflow.models import Variable
from airflow.utils.task_group import TaskGroup
from airflow.utils.dates import days_ago
from datetime import timedelta

default_args = {
    'owner': 'airflow',
    'depends_on_past': False,
    'email_on_failure': False,
    'email_on_retry': False,
    'retries': 1,
    'retry_delay': timedelta(minutes=5),
}

dag = DAG(
    'multi_api_pipeline',
    default_args=default_args,
    description='A pipeline that handles multiple APIs based on Airflow variables',
    schedule_interval=timedelta(days=1),
    start_date=days_ago(1),
    tags=['example'],
)

# Load the API config from Airflow variables
api_config = json.loads(Variable.get("api_config"))

start = DummyOperator(task_id='start', dag=dag)
end = DummyOperator(task_id='end', dag=dag)

# Function to create task group dynamically
def create_task_group(api_name, api_class, dag):
    with TaskGroup(api_name) as tg:
        get_token_task = PythonOperator(
            task_id=f'create_jwt_token_{api_name}',
            python_callable=eval(f"{api_class}.get_jwt_token"),
            dag=dag,
        )

        query_bq_task = PythonOperator(
            task_id=f'query_data_from_bq_{api_name}',
            python_callable=eval(f"{api_class}.fetch_data_from_bq"),
            dag=dag,
        )

        create_payload_task = PythonOperator(
            task_id=f'create_payload_{api_name}',
            python_callable=eval(f"{api_class}.create_payload"),
            dag=dag,
        )

        fetch_api_data_task = PythonOperator(
            task_id=f'fetch_data_from_api_{api_name}',
            python_callable=eval(f"{api_class}.fetch_data_from_api"),
            dag=dag,
        )

        insert_bq_task = PythonOperator(
            task_id=f'insert_data_to_bq_{api_name}',
            python_callable=eval(f"{api_class}.insert_data_to_bq"),
            dag=dag,
        )

        get_token_task >> query_bq_task >> create_payload_task >> fetch_api_data_task >> insert_bq_task

    return tg

# Create task groups for each enabled API
api_classes = {
    'Zoominfo': 'Zoominfo',
    'Buildwith': 'Buildwith'
}

for api, settings in api_config.items():
    if settings['enabled']:
        task_group = create_task_group(api, api_classes[api], dag)
        start >> task_group >> end

